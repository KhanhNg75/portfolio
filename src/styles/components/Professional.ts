import styled from "styled-components";

const Style = styled.div``;

const ProfessionalBlock = styled.ul`
    margin-top: 40px;
    columns: auto 2;
    list-style-type: none;
    padding-left: 0px;
`;

const ProfessionalItems = styled.li`
    position: relative;
    padding-left: 25px;
    margin-bottom: 20px;
    color: rgb(54, 54, 54);
`;

export { Style, ProfessionalBlock, ProfessionalItems };
